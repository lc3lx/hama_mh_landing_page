/**
 * Thin page adapter — route renders the Landing feature.
 * Keep this file tiny; all Landing logic lives in @features/landing.
 */
export { LandingFeature as LandingPage } from '@features/landing';
